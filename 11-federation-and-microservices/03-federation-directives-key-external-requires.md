# Federation Directives

- `@key(fields: "id")` – primary key for entity.
- `@external` – field provided by another subgraph.
- `@requires` – requires fields for computation.
- `@provides` – field exposed for other subgraphs.

> 📘 Next: [GraphQL Mesh & Schema Stitching](04-graphql-mesh-and-schema-stitching.md)
